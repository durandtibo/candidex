# Dependency compatibility testing workflow.
# Tests the package with different versions of optional dependencies.
# Ensures compatibility across supported dependency version ranges.

name: Test - Dependencies
on:
  workflow_call:
  workflow_dispatch:  # to trigger manually

permissions:
  contents: read

jobs:
  # Get dependency version information from config file
  get-versions:
    uses: ./.github/workflows/get-versions.yaml

  ########################
  #     Dependencies     #
  ########################

  beautifulsoup4:
    needs: get-versions
    uses: ./.github/workflows/test-code-dep.yaml
    with:
      package-name: beautifulsoup4
      package-versions: ${{ toJson(fromJson(needs.get-versions.outputs.package_versions).beautifulsoup4) }}

  coola:
    needs: get-versions
    uses: ./.github/workflows/test-code-dep.yaml
    with:
      package-name: coola
      package-versions: ${{ toJson(fromJson(needs.get-versions.outputs.package_versions).coola) }}

  polars:
    needs: get-versions
    uses: ./.github/workflows/test-code-dep.yaml
    with:
      package-name: polars
      package-versions: ${{ toJson(fromJson(needs.get-versions.outputs.package_versions).polars) }}

  rich:
    needs: get-versions
    uses: ./.github/workflows/test-code-dep.yaml
    with:
      package-name: rich
      package-versions: ${{ toJson(fromJson(needs.get-versions.outputs.package_versions).rich) }}


  #################################
  #     Optional dependencies     #
  #################################

  colorlog:
    needs: get-versions
    uses: ./.github/workflows/test-code-dep.yaml
    with:
      package-name: colorlog
      package-versions: ${{ toJson(fromJson(needs.get-versions.outputs.package_versions).colorlog) }}
